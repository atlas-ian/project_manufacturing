models:
  - name: raw_inventory
    description: "Table with all inventory information."
    columns:
      - name: INVENTORY_ID
        data_tests:
          - unique
          - not_null
  - name: raw_supplier
    description: "Table with all supplier information."
  - name: stg_production_orders
